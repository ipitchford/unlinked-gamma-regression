# Methodology specialist: internal editorial review

**Recommendation: Accept as an unrefereed candidate.** No blocking methodological defect found. Confidence: high for the Gamma invariant, exact fibers, and fixed-dictionary cutoff arguments; moderate for package-wide reproducibility, which was checked only on this host. This is producer-coordinated internal model review, not external independent peer review or proof-assistant verification.

## Submission and scope

Reviewed the frozen submission without modifying it. ZIP SHA256: `d916b6842c1b51b2f7b54ea686b5c8735497ae3929429c6aae6afe19a1a7584e`; included PDF SHA256: `f78c40e91a26e46ae5f15d2b10ccd53c86bdf78e3391925559479f1c20e71461`. Both hashes matched. Read the manuscript's proofs and original checkers, reproduction entry point, README and assurance statement. Did not consult other review reports. External prior-art attribution, publication readiness, PDF typography and empirical statistical performance are outside this role's audit.

## Findings and evidence

- **Exact fibers and minimality — no defect found.** Theorem 1 correctly uses a rational logarithmic derivative, whose residues at each nonzero inverse scale are strictly negative aggregate shapes. Negative scales cause no cancellation. Theorem 2's allocation bijection follows, including the zero block and zero-response edge case. Its full-support/minimality equivalence is valid specifically for the fixed dictionary: a proper subvector cannot attain its full positive shape mass. It would not imply minimality when the dictionary itself can change; the manuscript explicitly excludes that interpretation.
- **Sharp arithmetic criterion — no defect found.** Theorem 3 matches equal-shape labels separately within each scale block, including unused labels. In its converse, removing common indices preserves a genuine multiset discrepancy; separated squared-coefficient intervals exclude signed permutations even when block cardinalities differ. Corollary 5 appropriately uses the pigeonhole argument for exactly d distinct active scales.
- **Upper cutoff and reconstruction — no defect found.** Theorem 6 weights by the squared nonzero scale, so cumulant orders 2 through 2d+1 supply moments 0 through 2d-1. The difference has at most 2d nodes, enough for the stated Vandermonde argument. The rank and leading-block positivity used in Prony reconstruction are justified by positive weights and distinct real nodes. The manuscript appropriately requires exact shape comparisons and disclaims stability and efficient allocation.
- **Lower cutoff — no defect found.** Theorem 7 compares beta and minus beta in the same fixed positive dictionary. The displayed derivative identity gives alpha_i a_i^3 = 1/Q'(i^2); interpolation cancels precisely odd orders 3 through 2d-1 and leaves normalized order 2d+1 equal to one. Even cumulants agree under reflection and means vanish by centering. This covers d=1 with the empty cancellation range. Clearing denominators multiplies every normalized cumulant by the same positive integer and preserves the witness. Its scope is correctly limited to a worst-case consecutive segment with signed coefficients and distributional identification.
- **Reproduction — passed.** Executed `python3 -S reproduce.py` and `python3 -O -S reproduce.py`; both parsed outputs exactly matched `evidence/replay.json`. The complete manifest/hash verifier passed. As additional internal checks written separately from the submission's functions, direct rational odd-power summation verified the lower-bound formula for d=1 through 30; exhaustive signed/zero grids of 343 vectors each for shapes (1,1,2), (1,2,3), (1,1,3), and (2,3,5) verified moment-to-measure consistency, full-support invariance within fibers, and the distinct-active-scale orbit conclusion. These are bounded arithmetic checks, not universal proofs or external reproduction.

## Nonblocking observation

**Severity: low; optional documentation improvement.** The four advertised negative controls in `reproduce.py` are targeted semantic comparisons evaluated through `grid.require`/`rejected`. They demonstrate rejection of four explicitly altered identities, not a general mutation-testing harness or resistance to arbitrary checker faults. Their actual wording, “semantic negative controls,” and bounded-assurance statements are acceptable. Future documentation could call them “four targeted semantic controls” to make this distinction still clearer.

## Limitations

No fresh TeX build, second operating system, statistically perturbed input analysis, formal proof checking, or external literature verification was performed. Agreement with the producer's replay is corroboration of execution; the independently encoded bounded sums add arithmetic cross-checking but do not establish organizational independence. The written mathematical proofs carry the universal claims.
